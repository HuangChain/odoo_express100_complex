<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="kuaidi.list">
      <field name="name">kuaidi list</field>
      <field name="model">kuaidi.kuaidi</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="kuaidi.action_window">
      <field name="name">kuaidi window</field>
      <field name="res_model">kuaidi.kuaidi</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="kuaidi.action_server">
      <field name="name">kuaidi server</field>
      <field name="model_id" ref="model_kuaidi_kuaidi"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="kuaidi" id="kuaidi.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="kuaidi.menu_1" parent="kuaidi.menu_root"/>
    <menuitem name="Menu 2" id="kuaidi.menu_2" parent="kuaidi.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="kuaidi.menu_1_list" parent="kuaidi.menu_1"
              action="kuaidi.action_window"/>
    <menuitem name="Server to list" id="kuaidi" parent="kuaidi.menu_2"
              action="kuaidi.action_server"/>
    -->
  </data>
</odoo>